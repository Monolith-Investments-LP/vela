/target
# Cargo.lock is committed — this workspace ships a binary crate (`api`)
# and Fly builds against a reproducible lock file. If you split off a
# library crate, add its own local .gitignore.
*.log
*.prof
flamegraph.svg
profile.json.gz
perf.data
perf.data.old
*.trace
.DS_Store
.env
/data/
/snapshots/
.vercel
.env*.local

# TypeScript incremental build cache
*.tsbuildinfo

# Python bytecode / caches (from one-off patch scripts)
*.pyc
__pycache__/

# IDE
.idea/
.vscode/

# Local-only planning docs; never pushed.
BUILDPLAN.md
PAPER_REVISION_INPUTS.md

# Agent worktrees created by Claude sessions — never track.
.claude/
